{
  "name": "vocabularyapp",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently --names server,client -c blue,green \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "import": "npm run import --prefix server",
    "build": "npm install --prefix client && npm run build --prefix client && npm install --prefix server && cd server && npx prisma generate && npx tsc",
    "start": "cd server && npx prisma migrate deploy && node dist/src/index.js"
  },
  "engines": {
    "node": ">=20.19.0"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
